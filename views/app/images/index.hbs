<div class="container ">
    <h1>Mis imágenes</h1>
    {{# each images}}
    <div class="white border tr large-padding">
        <div class="row">
            <div class="col-md-10">
                <h2>{{this.title}}</h2>
            </div>
            <div class="col-md-2">
                <a href="/app/images/{{this._id}}/edit" class="right-space">Editar</a>
                <form action="/app/images/{{this._id}}?_method=DELETE" method="POST" class="link"><input type="submit" value="Eliminar" /></form>
            </div>
        </div>
    </div>
    {{/each}}
</div>